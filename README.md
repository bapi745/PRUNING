# PRUNING
Pruning in neural networks means removing unnecessary weights, neurons, or connections from a trained network to make it smaller, faster, and less prone to overfitting—without significantly hurting accuracy...


In this project we will be doing structured pruning. Structured pruning removes entire structures of a neural network—such as neurons, filters, channels, or blocks—instead of individual weights.
